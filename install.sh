brew update
brew install libiconv && xcode-select --install
sudo gem update --system
sudo gem install atomic
sudo gem install pg
gem install pg --user-install
sudo gem install bundler
gem install bundler
bundle config build.pg --with-pg-config=/Applications/Postgres.app/Contents/Versions/10/bin/pg_config
bundle install
